// src/server/billing/lemonsqueezy/handlers.ts
import type { LsWebhookBody } from "./webhook";

/**
 * Task 20 implements the full dispatcher; this stub keeps Task 19's route
 * type-checkable until then.
 */
export async function dispatchLsEvent(_body: LsWebhookBody): Promise<void> {
  // No-op until Task 20.
}
