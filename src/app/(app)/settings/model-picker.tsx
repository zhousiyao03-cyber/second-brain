"use client";

// Phase 4.10 stub: this component used the legacy billing.getAiChatModel /
// setAiChatModel procedures. Both are removed. Phase 7.x will replace it
// with role-based model selection in the new providers settings UI.
export function ModelPicker(_props: { provider: string }) {
  void _props;
  return null;
}
